import { ComponentStyleConfig } from '@chakra-ui/theme';

export const Text: ComponentStyleConfig = Object.freeze({
  variants: {
    test: () => ({
      fontSize: 'xl',
      fontWeight: 400,
    }),
  },
});
